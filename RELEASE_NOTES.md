# Reading Line 1.4.0 发布说明

这是首个完整整理的 GitHub 发布包，包含 KOReader 可直接安装的插件目录及中文使用说明。

## 安装文件

下载 `ReadingLine-v1.4.0.zip`，解压后将 `readingline.koplugin` 复制到 KOReader 的 `plugins` 目录，然后重启 KOReader。

## 升级提醒

旧版若安装在 `simplebookshelf.koplugin` 中，请先移除旧目录，避免重复加载。历史设置仍保存在 `simplebookshelf.lua`，升级后会继续使用。

## 已验证设备

- Kindle Paperwhite 6（KPW6）
- Kindle Scribe
